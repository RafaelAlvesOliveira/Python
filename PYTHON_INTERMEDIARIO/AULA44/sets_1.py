# Add (adicional), update (atualiza), clear, discar
# union | (une)
# Intersection & (todos os elementos presentes nos dois sets)
# difference - Elementos apenas no set da esquerda)
# symmetric_difference ^ (Elementos que estão nos dois sets, mas não em ambos)

s1 = {1, 2, 3, 4, 5, 6}

for v in s1:
    print(v)

# É possível iterar sobre os valores dentro de 'set', mas não é possível
# acessar o valor diretamente. Se não for adicionado nenhum valor dentro
# do 'set', se cria um dicionário. Não tem como criar um 'set' em branco.
