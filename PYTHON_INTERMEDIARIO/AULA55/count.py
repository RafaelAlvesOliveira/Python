"""
Count - Itertolls
"""
from types import GeneratorType
variavel = zip('Alo', 'Alo')
# print(list(variavel))
# Isso é um iterável, a função 'zip' retorna um iterador,
# que entrega um valor de cada vez.
print(next(variavel))
print(next(variavel))
print(next(variavel))
